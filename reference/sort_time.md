# Sort a stars object along its time dimension

The amplitudes in \[match_times()\] are arranged by time, so the field
they are correlated against must be too – storage order is not
guaranteed.

## Usage

``` r
sort_time(dat)
```
