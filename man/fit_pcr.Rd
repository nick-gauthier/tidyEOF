% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/couple.R
\name{fit_pcr}
\alias{fit_pcr}
\title{Fit the OLS coefficient matrix for PCR coupling}
\usage{
fit_pcr(pred_amps, resp_amps, center = TRUE)
}
\arguments{
\item{pred_amps}{Predictor amplitude matrix (time x k_pred)}

\item{resp_amps}{Predictand amplitude matrix (time x k_resp)}

\item{center}{Logical; center both sides before fitting (default TRUE)}
}
\value{
List with `coefficients` (k_pred x k_resp), `xcenter`, `ycenter`
  (each a named numeric vector when centered, or `FALSE`)
}
\description{
Ordinary least squares of the predictand PC amplitudes on the predictor PC
amplitudes. The EOF truncation already performed the dimension reduction, so
this is plain (optionally centered) multivariate regression. The fit is
rank-safe: a rank-deficient predictor matrix (e.g. `k_pred >= n_times` or
collinear amplitudes) aborts rather than returning `NA` coefficients.
}
\keyword{internal}
